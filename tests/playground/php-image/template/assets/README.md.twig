## Image {{ orca.image }}

## docker-compose.yml

```ruby
app:
    container_name: app
    image: dasistweb/{{ orca.image }}:{{ orca.tag }}
    ports:
        - "1000:80"
        - "1022:22"
    environment:
        - XDEBUG_ENABLED=0
```