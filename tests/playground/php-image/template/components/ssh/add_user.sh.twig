
RUN adduser --disabled-password --uid 5577 --gecos "" {{ user }} \
    && usermod -a -G www-data {{ user }} \
    && usermod -m -d /var/www {{ user }} \
    && echo "{{ user }}:{{ pwd }}" | chpasswd \
    && sed -i 's/{{ user }}:x:5577:5577:/{{ user }}:x:33:33:/g' /etc/passwd
